schema: src/services/graphql/schemas/schema.graphql
generates:
  ./src/services/graphql/generated/resolvers-types.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: true